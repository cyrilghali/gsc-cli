export type PainSignal = {
  source: string
  term: string
  url: string
  story_title: string
  excerpt: string
  matched_phrase: string
  weight: number
  workaround_detected: boolean
  created_at: string
}
